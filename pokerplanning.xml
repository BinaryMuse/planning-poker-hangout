<?xml version="1.0" encoding="UTF-8"?>
<Module>
  <ModulePrefs title="Poker Planning">
    <Require feature="rpc" />
    <Require feature="views" />
  </ModulePrefs>
  <Content type="html">
    <![CDATA[
      <script src="//plus.google.com/hangouts/_/api/v1/hangout.js"></script>
      <script src="//ajax.googleapis.com/ajax/libs/angularjs/1.2.10/angular.min.js"></script>

      <div id='app-root'>
        <div ng-controller="DebugController">
          <pre>
            {{poker | json}}
          </pre>
        </div>

        <div ng-controller='ModeratorController'>
          <div ng-if="poker.moderator.id == poker.me.id">
            <h1>Moderator Controls</h1>
            <div>
              Points: <input type='text' ng-model='poker.availablePoints' ng-list ng-change='setPoints()'>
            </div>
            <div>
              Current Item Description:<br>
              <textarea ng-model="description" ng-change="setDescription(description)"></textarea>
            </div>
            <div>
              Votes revealed: <input type='checkbox' ng-model='poker.votesRevealed.value' ng-change='setPointsRevealed()'>
            </div>
          </div>

          <div ng-if="poker.moderator.id != poker.me.id">
            <button ng-click='becomeModerator()'>Become Moderator</button>
          </div>
        </div>

        <div ng-controller="VoteController">
        </div>

        <div ng-controller="ParticipantsController">
          <ul>
            <li ng-repeat="person in poker.participants">
              {{ person.person.displayName }} -
              <span ng-if="!poker.votesRevealed.value">
                <span ng-show="!poker.votes[person.id]">...</span>
                <span ng-show="poker.votes[person.id]">voted</span>
              </span>

              <span ng-if="poker.votesRevealed.value>
                {{ poker.votes[person.id] }}
              </span>
            </li>
          </ul>
        </div>
      </div>

      <script>
      var app = angular.module('planning-poker', []);

      app.factory('planningPoker', function() {
        return {
          participants: [],
          availablePoints: [0, 1, 2, 3, 5, 8, 13],
          moderator: { id: null },
          description: { text: null },
          votesRevealed: { value: false },
          votes: {}
        };
      });

      app.run(function($log, $rootScope, planningPoker) {
        $log.debug('planning poker started');

        $rootScope.$apply(function() {
          planningPoker.participants = gapi.hangout.getParticipants();
          planningPoker.me = gapi.hangout.getLocalParticipant();
        });

        gapi.hangout.data.onStateChanged.add(function(event) {
          $rootScope.$apply(function() {
            var state = gapi.hangout.data.getState();
            for (key in state) {
              planningPoker[key] = angular.fromJson(state[key]);
            }
            $log.debug('State updated to:');
            $log.debug(planningPoker);
          });
        });

        gapi.hangout.onParticipantsChanged.add(function(event) {
          $rootScope.$apply(function() {
            planningPoker.participants = event.participants;
          });
        });
      });

      app.controller('DebugController', function($scope, planningPoker) {
        $scope.poker = planningPoker;
      });

      app.controller('ModeratorController', function($scope, planningPoker) {
        $scope.poker = planningPoker;

        $scope.setPoints = function() {
          gapi.hangout.data.setValue('availablePoints', angular.toJson(planningPoker.availablePoints));
        };

        $scope.setDescription = function() {
          gapi.hangout.data.setValue('description', angular.toJson(planningPoker.description));
        };

        $scope.setVotesRevealed = function() {
          gapi.hangout.data.setValue('votesRevealed', angular.toJson(planningPoker.votesRevealed));
        };

        $scope.becomeModerator = function() {
          poker.moderator.id = poker.me.id;
          gapi.hangout.data.setValue('moderator', angular.toJson(planningPoker.moderator));
        };
      });

      function initApp() {
        var root = document.getElementById('app-root');
        angular.bootstrap(root, ['planning-poker']);
      }

      gapi.hangout.onApiReady.add(initApp);
      </script>
    ]]>
  </Content>
</Module>
